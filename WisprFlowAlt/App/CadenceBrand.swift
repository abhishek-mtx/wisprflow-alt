import SwiftUI

/// System palette — no neon, no page gradients.
enum CadenceBrand {
    static let name = "Cadence"
    static let tagline = "On-device dictation"

    static let accent = Color.accentColor
    static let listening = Color.blue
    static let command = Color.indigo
    static let polishing = Color.orange
    static let error = Color.red
}
